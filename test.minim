int count(int n) {
    if (n <= 1) {
        return n;
    }
    return n + count(n-1);
}

void main() {
    int c;
    while c < 25 {
        disp << c;
        disp << " : ";
        disp << count(c);
        disp << "\n";
        c++;
    }
}